package com.onirun.screens.login

/**
 * An enum used to know what message to display to the user when he arrives on the login screen
 */
enum class LoginCause {

    DEFAULT,
    DEEP_LINK,
    FRIENDS,
    EVENTS,
    NOTIFICATION

}